version: '3.8'

services:

  selenium-hub:
    image: selenium/standalone-chrome:latest
    container_name: selenium-hub
    ports:
      - "4444:4444"

  orangehrm-test:
    build: .
    container_name: orangehrm-test
    depends_on:
      - selenium-hub